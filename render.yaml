services:
  - type: web
    name: by-pro-accounts-login
    env: node
    plan: free
    buildCommand: cd src && npm install
    startCommand: cd src && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.0.0
